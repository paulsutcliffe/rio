= form_for @diapositiva do |f|
  - if @diapositiva.errors.any?
    #error_explanation
      %h2= "#{pluralize(@diapositiva.errors.count, "error")} prohibited this diapositiva from being saved:"
      %ul
        - @diapositiva.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :slide
    = f.text_field :slide
  .field
    = f.label :titulo
    = f.text_field :titulo
  .field
    = f.label :link
    = f.text_field :link
  .field
    = f.label :video
    = f.text_field :video
  .actions
    = f.submit 'Save'
